apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis-replica-pdb
  namespace: mahjong-solitaire
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: redis
      role: replica
